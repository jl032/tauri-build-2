{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Zlib",
  "version": "0.1.0",
  "identifier": "com.jonat.zlib",
  "build": {
    "devUrl": "https://z-library.sk/",
    "beforeBuildCommand": "",
    "frontendDist": "https://z-library.sk/"
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "title": "Zlib",
        "url": "https://z-library.sk/",
        "width": 1200,
        "height": 800, 
        "initScript": "window.addEventListener('DOMContentLoaded', () => { document.addEventListener('click', (e) => { const a = e.target.closest('a'); if (a && a.href && !a.href.startsWith(location.origin)) { e.preventDefault(); window.__TAURI__.shell.open(a.href); } }); const nav = document.createElement('div'); nav.style.cssText = 'position:fixed;top:0;left:0;right:0;height:40px;background:#1a1a1a;display:flex;align-items:center;padding:0 8px;gap:8px;z-index:999999;'; const btn = (label, action) => { const b = document.createElement('button'); b.innerText = label; b.style.cssText = 'background:#333;color:#fff;border:none;border-radius:4px;padding:4px 10px;cursor:pointer;font-size:14px;flex-shrink:0;'; b.onclick = action; return b; }; const input = document.createElement('input'); input.value = location.href; input.style.cssText = 'flex:1;background:#333;color:#fff;border:none;border-radius:4px;padding:4px 10px;font-size:13px;outline:none;'; input.addEventListener('keydown', (e) => { if (e.key === 'Enter') { let url = input.value; if (!url.startsWith('http')) url = 'https://' + url; location.href = url; } }); nav.appendChild(btn('←', () => history.back())); nav.appendChild(btn('→', () => history.forward())); nav.appendChild(btn('⟳', () => location.reload())); nav.appendChild(input); document.body.style.paddingTop = '40px'; document.body.appendChild(nav); });"
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  },
  "plugins": {
    "shell": {
      "open": true
    }
  }
}
